{deps, [
    {cpflib, {git, "git://github.com/aialferov/cpflib", {tag, "master"}}}
]}.

{shell, [{apps, ['{{name}}']},
         {config, "priv/{{name}}.config"}]}.

{provider_hooks, [{post, [{compile, escriptize}]}]}.

{escript_emu_args, "%%! -start_epmd false"}.
